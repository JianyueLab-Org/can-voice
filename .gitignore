*.pem
.temp/
.DS_Store

# Rust 构建产物（P3 起）。Go 那半边没有对应物，所以此前不需要这一条。
/target/

# Tauri 客户端（P4 起）。三种产物，三个位置：
# 前端的 node_modules/dist、Rust 侧的 target。`bun.lock` 要提交。
apps/*/node_modules/
apps/*/dist/
apps/*/src-tauri/target/
apps/*/src-tauri/gen/

# can-voice-autoupdate 不在 workspace 里（它依赖 tauri），所以它有自己的
# target/ 和 Cargo.lock。库不锁版本——锁在四个应用那边。
crates/can-voice-autoupdate/target/
crates/can-voice-autoupdate/Cargo.lock
