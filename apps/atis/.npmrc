# can-ui 装在 GitHub Packages 上，而 GitHub 的 npm registry **即使是公开包也要求
# 带令牌**——这是 GitHub 的规矩，不是我们的选择。所以这个文件必须提交：
# 它写的是 registry 地址，不是凭据。
#
# `${GITHUB_TOKEN}` 在安装时才从环境里读。本地是一个带 `read:packages` 的个人
# 令牌；CI 里是工作流自带的 `secrets.GITHUB_TOKEN`，它本来就有这个权限。
@jianyuelab-org:registry=https://npm.pkg.github.com
//npm.pkg.github.com/:_authToken=${GITHUB_TOKEN}
